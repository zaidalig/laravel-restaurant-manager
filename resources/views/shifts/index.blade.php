@extends('layouts.app')@section('title','Shifts')@section('page_title','Staff Shifts')
@section('content')<div class="d-flex justify-content-between mb-3"><a href="{{ route('shifts.create') }}" class="btn btn-primary">Schedule Shift</a>
<form method="GET" class="d-flex gap-2"><input type="date" name="date" class="form-control w-auto" value="{{ request('date') }}"><button class="btn btn-dark">Filter</button>@if(request()->filled('date'))<a href="{{ route('shifts.index') }}" class="btn btn-outline-secondary">Clear</a>@endif</form></div>
<div class="card card-table border-0"><div class="table-responsive"><table class="table mb-0"><thead class="table-light"><tr><th>Staff</th><th>Date</th><th>Start</th><th>End</th><th>Position</th><th>Status</th><th></th></tr></thead><tbody>@foreach($shifts as $shift)<tr><td>{{ $shift->user->name }}</td><td>{{ $shift->shift_date->format('M d, Y') }}</td><td>{{ $shift->start_time }}</td><td>{{ $shift->end_time }}</td><td>{{ $shift->position ?? '-' }}</td><td>{{ $shift->status }}</td><td class="text-end"><div class="table-actions"><a href="{{ route('shifts.edit',$shift) }}" class="btn btn-sm btn-outline-primary" title="Edit"><i class="fa-solid fa-pen"></i></a> <button class="btn btn-sm btn-outline-danger" data-bs-toggle="modal" data-bs-target="#deleteModal" data-url="{{ route('shifts.destroy',$shift) }}" data-name="shift" title="Delete"><i class="fa-solid fa-trash"></i></button></div></td></tr>@endforeach</tbody></table></div>@include('components.table-pagination', ['paginator'=>$shifts, 'sorts'=>['shift_date'=>'Date','created_at'=>'Created','status'=>'Status']])</div>@endsection
